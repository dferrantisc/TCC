CREATE TABLE ProprietarioFunci(
	idAdm SERIAL PRIMARY KEY,
	loguser VARCHAR(200) NOTNULL, 
	logpass VARCHAR(100) NOTNULL,
	Nome VARCHAR(100) NOTNULL,
	Sobrenome VARCHAR(100) NOTNULL,
	Email VARCHAR(100)
	
),
CREATE TABLE UsuarioCliente(
	idUC SERIAL PRIMARY KEY,
	NomeCompleto VARCHAR(200) NOTNULL,
	CPF VARCHAR(15) NOTNULL,
	Telefone VARCHAR(20) NOTNULL,
	DatadeNascimento DATE NOTNULL,
	EmailUC  VARCHAR(100)
),

CREATE TABLE Produtos(
	idPdt SERIAL PRIMARY KEY,
	NomePdt VARCHAR(200) NOTNULL,
	Categoria VARCHAR(100) NOTNULL,
	Preço FLOAT(24)
),
CREATE TABLE Sorteios(
	idSort SERIAL PRIMARY KEY,
	Ganhadores VARCHAR(200) NOTNULL,
	Participantes VARCHAR NOTNULL,
	FOREIGN KEY (idUC) REFERENCES UsuarioCliente (idUC)
);